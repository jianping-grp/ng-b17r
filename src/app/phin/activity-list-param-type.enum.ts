export enum ActivityListParamType {
  target_tid = 1,
  molecule_molregno,
  scaffold,
  mix, // the combinations of the above type or other
  tid_moleculeProperty, // target_tid and moleculeProperty
}
