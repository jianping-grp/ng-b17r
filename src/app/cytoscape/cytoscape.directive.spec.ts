import { CytoscapeDirective } from './cytoscape.directive';

describe('CytoscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new CytoscapeDirective();
    expect(directive).toBeTruthy();
  });
});
