import { JsmeDirective } from './jsme.directive';

describe('JsmeDirective', () => {
  it('should create an instance', () => {
    const directive = new JsmeDirective();
    expect(directive).toBeTruthy();
  });
});
