import { ViewPageByIdDirective } from './view-page-by-id.directive';

describe('ViewPageByIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewPageByIdDirective();
    expect(directive).toBeTruthy();
  });
});
