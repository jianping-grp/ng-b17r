import { AssayToTargetNamePipe } from './assay-to-target-name.pipe';

describe('AssayToTargetNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AssayToTargetNamePipe();
    expect(pipe).toBeTruthy();
  });
});
