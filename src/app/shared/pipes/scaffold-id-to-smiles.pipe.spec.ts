import { ScaffoldIdToSmilesPipe } from './scaffold-id-to-smiles.pipe';

describe('ScaffoldIdToSmilesPipe', () => {
  it('create an instance', () => {
    const pipe = new ScaffoldIdToSmilesPipe();
    expect(pipe).toBeTruthy();
  });
});
