import { PhinMolidToSmilesPipe } from './phin-molid-to-smiles.pipe';

describe('PhinMolidToSmilesPipe', () => {
  it('create an instance', () => {
    const pipe = new PhinMolidToSmilesPipe();
    expect(pipe).toBeTruthy();
  });
});
