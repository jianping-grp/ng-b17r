import { AsterishToRPipe } from './asterish-to-r.pipe';

describe('AsterishToRPipe', () => {
  it('create an instance', () => {
    const pipe = new AsterishToRPipe();
    expect(pipe).toBeTruthy();
  });
});
